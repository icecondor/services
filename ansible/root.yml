---
- hosts: boxes
  tasks:
# User
    - user: name=devops createhome=yes generate_ssh_key=no groups="sudo" shell="/bin/bash"
    - copy: src=../keys/bashrc dest=/home/devops/.bashrc
    - copy: src=../keys/id_rsa dest=/home/devops/.ssh/
    - copy: src=../keys/id_rsa.pub dest=/home/devops/.ssh/
    - copy: src=../keys/authorized_keys dest=/home/devops/.ssh/
    - copy: src=../keys/sudoers dest=/etc/
